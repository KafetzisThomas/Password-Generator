from setuptools import setup, find_packages

setup(
    name="pass_generator",
    version="1.0",
    description="Generates random strings representing passwords.",
    author="KafetzisThomas",
    packages=find_packages(),
)
